<!DOCTYPE html>
<html>
	<head>

		<meta charset="UTF-8">

		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

		<title>Nouvelle décision</title>
	</head>
	<body>
		<h1>Bonjour
			{% if impactedUserRole === {{ constant('App\\Entity\\Interaction::DECISION_IMPACTED', interaction) }} %}
				{{ impactedUser.user.firstname }}
				{{ impactedUser.user.lastname }}
				, une nouvelle decision où tu es impacté "
				{{ decision.title }}
				" vient tout juste d'être créée !
			{% endif %}
			{{ impactedUser.user.firstname }}
			{{ impactedUser.user.lastname }}
			, une nouvelle décision où tu es expert "
			{{ decision.title }}
			" vient tout juste d'être créée !

		</h1>
	</body>
</html>
